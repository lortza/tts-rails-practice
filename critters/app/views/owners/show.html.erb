<p id="notice"><%= notice %></p>

<h1><%= @owner.name %></h1>

<p>
  <strong>Lives in:</strong>
  <%= @owner.city %>, <%= @owner.country.name %>
</p>

<p>Critters:</p>
  <ul>
  <% @owner.critters.order("name").each do |x| %>
  <li> <%= link_to x.name, x %> </li>
  <% end %>
  </ul>


<%= link_to 'Edit', edit_owner_path(@owner) %> |
<%= link_to 'Back', owners_path %>

<p id="notice"><%= notice %></p>  

<h1><%= @country.name %></h1>

<p>
  <strong>All Residents of <%= @country.name.capitalize %>:</strong>
  <ul>
  <% @country.guardians.order("name").each do |x| %>
  <li> <%= x.name %> </li>
  <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_country_path(@country) %> |
<%= link_to 'Back', countries_path %>
